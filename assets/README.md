# Assets

This directory intentionally contains only documentation and manifests. Robot USDs, room USDs, generated merged assets, policy checkpoints, videos, and logs are not committed.

Expected local layout after asset preparation:

```text
assets/
  robots/
    Go2/
    Piper/
    Go2Piper/
  room/
  cfg/
```

See `docs/02_assets.md` and `assets/manifest.example.yaml`.
